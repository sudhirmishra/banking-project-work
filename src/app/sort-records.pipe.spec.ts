import { SortRecordsPipe } from './sort-records.pipe';

describe('SortRecordsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRecordsPipe();
    expect(pipe).toBeTruthy();
  });
});
