import { TxDescriptionPipe } from './tx-description.pipe';

describe('TxDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new TxDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
